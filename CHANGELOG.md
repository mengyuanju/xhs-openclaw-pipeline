# Changelog

## [Unreleased]

### Added

- Query 审核通过后自动进入独立小红书搜索队列：可见浏览器保留 `xsec_token`、`xsec_source` 等完整访问参数，跨滚动去重后按点赞量排序；管理员可在“生产配置”设置每个 Query 保留 `1–10` 条（默认 `3` 条），任务领取时冻结本次条数。
- “全部作业”新增重复 Query 清理预览与确认：仅处理同业务上下文且尚未产生执行进展的重复任务，保留代表任务及已有成果，并通过预览指纹、幂等请求、事务锁和审计记录保证操作可追溯。
- 独立预览服务新增管理员登录、HttpOnly Session、失败锁定、分权限 API Key、限流及审计日志；浏览器管理接口与 Bearer 服务接口分离，公开预览链接继续支持匿名访问。
- Query 词包名称贯穿正式任务、非盲文案抽检与交付池：支持按名称搜索/筛选，交付可按词包完整导出，ZIP 按词包目录归类，Excel 增加词包名称首列；独立盲评继续隐藏词包来源。
- 执行机管理新增安全删除入口：仅允许移除离线且无运行任务的执行机，保留历史任务记录；同一执行机重新启动后会自动恢复显示。
- 管理员可在“全部作业”的排队任务行直接“废弃”，任务退出队列后原位置切换为“永久删除”；批量操作采用相同两步流程，永久删除继续要求删除二级密码。
- 管理员任务工作台支持勾选最多 20 条已失败、已审核或已废弃任务，输入一次删除二级密码后批量永久删除；中心服务只校验一次凭据，在同一数据库事务中删除符合条件的任务，并对素材执行隔离、失败恢复及提交后清理。
- 管理员创建笔记时可按批次启用“免人工文案审核，直接生图”；免审任务必须显式指定负责人，中心校验管理员权限并保存任务策略，文案完成后原子放行入队。该选项默认关闭，旧任务和普通账号继续人工审核。
- 生产配置新增 10 类、27 个版本化布局模板，支持 JSON 导入及模型候选自动入库；视觉规划按内容选版式、保存统一风格，生图前持久化并在续跑时复用中心冻结规划。
- 工作台任务列表及详情增加生图失败断点续跑；图片审核显示当前版本质检摘要、逐项证据和图片跳转，并固定底部审核操作。
- 任务资产增加按需 WebP 缩略图、鉴权后的私有 ETag 缓存和轻量任务归属查询，减少预览传输与数据库读取。
- 页面控件统一接入共享组件：下拉框、搜索及清除、输入框、开关、日期与颜色选择、折叠详情和进度条；支持键盘操作、表单重置与窄屏显示。
- 人工归档的图片审核新增“审核通过”“重试生图”“废弃”，管理员和审核员可处理所有创建者的归档任务；通过后标记“已审核”，记录审核人和时间。
- 作业中心新增“已完成”列表，显示图片审核通过的图文，保留详情预览和资源包下载，创建者也可在个人列表查看。
- 管理员新增“全部作业”，覆盖所有任务状态和历史记录，支持按创建者当前角色、任务状态和 Query 组合筛选及服务端分页；中心角色筛选仅对管理员开放。
- 管理员侧栏新增“执行机管理”，集中展示全部执行机的在线状态、最后心跳、文案与生图并发占用，并每15秒自动刷新。
- 个人作业中心和生图中列表新增“重试生图”；有已审核文案的任务可作废当前生图执行并重新进入全局待生图队列。
- 待文案审核详情新增任务级“AI生成”开关；审核提交后由中心服务持久化并冻结到生图执行快照，执行机据此控制本次图片的“AI生成”标识。
- 执行机生成文案前对全部优秀案例摘要进行模型评分，仅引用至少70分的最高分案例完整分析，并保存匹配结果与版本依据。
- 生产配置页新增独立联网搜索面板，本地和远端中心均可保存搜索提供方、DeepSeek 模型及超时，执行机优先使用保存的配置。
- 新增独立联网搜索配置，可在保留原有文案、审核和生图流程的同时切换为 DeepSeek；默认使用 DeepSeek。
- 恢复完整知识库工作台及常用分析 Prompt 管理，支持独立 SQLite 与远端中心两种运行方式。
- 新增知识库只读迁移预检、完整导入及重复导入保护，保留旧文案、标签和分析元数据。
- 图片生成中的运行现在可以手动取消；服务重启后遗留的“生成中”记录也能正常收尾。

### Changed

- Query 词包筛选通过后直接原子创建未分配的正式任务并进入共享文案队列；知识库、Query 词包、交付池及流程质检配置收紧为管理员专用，审核员保留作业与文案质检权限。
- 统一文案审核语义：机器原稿仅 `3` 分可不修改通过，`2` 分或 `2.5` 分须实际返修并在确认达标后记为最终 `3` 分；普通批次按配置抽检，返工稿强制复检通过后才进入待生图队列，图片仍允许 `2.5` 分或 `3` 分通过。
- DeepSeek 搜索模型改用安全格式校验而非固定版本白名单，默认使用已验证能完成服务端 `web_search` 的 `deepseek-v4-pro`；后续新模型经联网搜索验证后可直接通过生产配置或执行机环境启用。
- Codex 主模型明确满载时，文本、审核、视觉和检索调用自动切换到可配置的容量备用模型，并通过共享模型级熔断避免重复撞击；图片生成和改图继续固定原驱动模型，不参与降级。
- 个人作业中心及服务端任务分页将“人工归档”排在“待文案审核”之后，同一优先级仍按创建时间倒序排列。
- 任务详情图片预览支持上一张、下一张连续查看；“AI生成水印”开关改为高对比滑动状态，并明确显示已开启或已关闭。
- Query 创建不再绑定文案执行机，未领取任务统一进入共享“待文案执行”队列；执行机按自身剩余并发容量原子领取，文案重试也回到共享队列。
- 登录页未指定返回地址或从站点首页进入时，默认落地到个人作业中心；首次改密流程与其它指定返回地址仍按原有规则处理。
- 客户端任务列表自动轮询统一为每 30 秒一次，覆盖作业中心、文案与生图历史、批量文案审核队列，并同步页面刷新提示。
- 文案执行中和失败任务均支持直接重试；重试后回到共享文案队列，等待任一有空闲并发容量的执行机领取。
- 执行机文案知识取消字符截断，文本调用取消30,000字符输入上限；案例评分遇到模型容量不足时按完整案例分批。
- 将“批量生成图文”拆分为“批量生成文案”和“批量生成图片”两个入口。
- 批量文案完成后必须逐条人工质检；批量图片只允许选择人工质检通过的文案记录。
- 单张和批量图片生成移除 Mock 模式，统一调用真实模型并显式确认费用。

### Fixed

- 小红书搜索结果不再保存无法直接访问的裸链接；交付前会等待本次搜索完成，并在交付表格中写入带签名参数的有效链接及对应点赞量。
- 优秀案例评分为空、不完整或格式无效时跳过案例参考并继续正常生成，单条案例超过模型容量时同样跳过；保存跳过原因，避免从部分评分中误选案例。
- 配图数字校验识别正文中的中文、全角数字并忽略要点列表编号，修复“五/5”等写法误拦截；完整数字及百分比独立匹配，入口和视觉规划输出沿用同一规则，失败时显示具体页码、字段和原句。
- 管理员可在全部作业查到生图失败和已废弃任务；工作台自动刷新失败会保留并标记旧结果，旧中心不再被全部作业页误当成完整分页数据源。
- 知识库页面与读写接口向中心服务传递当前登录用户、角色和凭据版本，修复启用用户管理后的身份缺失报错，并恢复审核员的视觉知识分析入口。
- 文案字数校验统一报告400–600字范围及实测长度，超长修订仅返回正文并保留其他字段，明确英文和代码的计数方式与删减目标；配图输出提示补齐已有字段长度限制。
- 中心响应读取超时或格式损坏时明确报错，避免将领取失败误判为队列为空。
- 恢复管理员左侧导航中的“生产配置”入口，可直接进入联网搜索配置面板。
- 批量图片页现在展示并持续刷新真实生成历史，每条任务从启动时即可通过运行 ID 打开详情。

## [executor-concurrency-2026-09-05] - 2026-09-05

### Added

- 文案与图片执行机分别按 env 配置维护有界任务池，按空闲容量批量领取、并发执行，任务完成后立即补位。
- 中心保存节点容量并支持幂等批量领取，防止重复领取和超限；新增增量迁移 `0007`、`0008`。
- Codex 总调用与图片调用许可分别可配置，跨进程共享；新增本机隔离的真实模型性能测试脚本与测试报告。

### Fixed

- 区分新图生成和图片编辑指令，通过 Codex app-server 接收原生图片完成事件，修复已生成图片却无法接收的问题。
- 正确处理已经恢复的网络重连，保留有界、脱敏的失败证据；修复 DeepSeek 搜索 JSON 结构和代码块兼容问题。
- 并发执行的失败回报、工作目录和恢复检查点按执行 ID 隔离，避免重跑模型或清理其他执行的文件。

### Upgrade

- 先升级中心迁移和 `executorConcurrency` 能力，再更新执行机；任务默认并发仍为文案 1、图片 1，Codex 默认总许可 2、图片许可 1。
- 真实测试达到任务并发 3/2，6 张图片全部通过原有校验；3 个文案选题分次复验成功。时长、局部修复次数和验证边界见 [真实测试报告](docs/executor-concurrency-live-results.md)。

## [0.2.0] - 2026-09-02

### Added

- 新增独立的批量生成模式，可批量执行文案生成、图片生成或完整图文生成。
- 新增批次进度、逐项状态、失败重试、结果筛选及批量下载入口。

### Changed

- 单次文案生成不再自动执行文本审核或二次改写，首稿直接保存并保持既有接口兼容字段。
- 单次文案与图片生成入口保持独立，原有单次流程不受批量模式影响。

[0.2.0]: https://github.com/mengyuanju/xhs-openclaw-pipeline/releases/tag/v0.2.0
