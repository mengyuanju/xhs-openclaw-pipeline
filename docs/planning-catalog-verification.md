# Query 与图片规划配置验证记录

日期：2026-09-07。目录：`C:\Users\HMCD-0005\Desktop\xhs-planning-config`。

## 自动验证

- 应用全量：`node --test --test-concurrency=2 'tests/*.test.mjs'`，954 通过、0 失败、0 跳过。
- 中心全量：`node --test --test-concurrency=2 'server/tests/*.test.mjs'`，147 通过、0 失败、0 跳过。
- `npm run typecheck` 通过。
- `npm run build` 通过，构建使用单工作进程，测试限制并发并降低进程优先级。
- 测试使用 fake client、内存或临时数据库，没有调用真实模型。
- 最终补查局域网 HTTP 的 ID 生成回退与目录合同，15 项通过；编辑器复用项目现有回退生成器。
- 独立代码审查完成，无剩余阻断问题；审查者只读复跑规划、检查点及模拟执行器测试，27/27 通过。`git diff --check` 无异常。

覆盖 Query 标点与换行、目录校验和旧 ID 兼容、配置存储、固定输出结构、自定义类型选择、实际生图 API 页结构校验、描述进入视觉/生图提示词、历史页面、独立生图导入、本地任务续跑快照、模拟执行器的配置和图片资产合同。

## 浏览器验证

生产构建在 `127.0.0.1:3007` 启动，连接 `127.0.0.1:3008` 的内存假中心，仅使用测试账号和测试任务。

- Query 同一行含中英文逗号仍为一条；两行识别为两条。
- 配置页新增类型、名称/描述编辑、启用、排序、保存成功。
- 新启用类型未配置适用布局时，保存明确提示需补充布局；补充关联布局后可保存。
- 新增“避坑页”可在审核页面的内容页下拉框选择，显示其描述；首图只允许封面类型。
- 页签 ArrowRight 切换选中状态并正确移动焦点。
- 320、768、1024、1440 像素宽度检查无页面横向溢出。
- 配置及审核流程浏览器控制台无 error/warn。

## 服务隔离

原目录的执行器 PID 12668、Next 启动器 PID 19532、服务 PID 19420 的启动时间未变化。未写入原目录的源码、配置、数据库或构建产物，未调用其 worker、重启或部署。浏览器预览和验证进程均属于独立目录。

原目录有其他并行工作产生的未提交修改，未覆盖或合并。本次功能尚未在当前服务中生效。

验证后已停止独立预览的 3007/3008 端口服务；原 3001 服务继续由 PID 19420 监听。验证日志归档在隔离目录的 `.codex_artifacts/planning-verification/`，不纳入生产代码提交。
