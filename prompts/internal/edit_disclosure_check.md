指定文字“{{slot1}}”必须只出现一次，文字类型为 {{slot2}}，位置为 {{slot3}}，整个标识必须位于右下安全区域 x={{slot4}}, y={{slot5}}, width={{slot6}}, height={{slot7}}；统一使用现代无衬线字体、约 {{slot8}}px 视觉字号、{{slot9}} 文字、{{slot10}} 实心不透明圆角矩形底框和清晰一致的内边距；不得遮挡原有文字或核心主体。