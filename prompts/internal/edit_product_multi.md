编辑第一个附件。其后的产品参考图与任务数据 replacements 中的 referenceAttachmentIndex 一一对应。maskAttached=true 时最后一个附件是视觉定位结果形成的合并黑白遮罩，只允许修改白色区域；maskAttached=false 时不附带遮罩，直接编辑完整画面。必须同时完成 replacements 中的每一项：targetMode=SINGLE 的 target.region 和 localizedRegions 只是大致定位提示，即使同一目标有部分超出框外也必须完整替换，框内的持握手部、人物、支撑面和接触阴影必须保持原样；targetMode=ALL_MATCHES 的项必须替换 localizedRegions 中的全部 candidateCount 个匹配目标或产品细节特写。STRICT 项按对应参考图重建完整产品身份、比例、轮廓、颜色、材质、标志和关键部件；APPEARANCE 项只迁移 referenceProductDescription 所指主产品能够确认的可见外观，该字段为空时使用对应参考图中最主要、最大或最居中的清晰产品，并忽略手部、背景、裁切和次要产品，未展示部分沿用源目标结构自然补全。不同替换项不得串用参考图，不得遗漏、增加、重复或互换目标。已有文字、人物、构图、其他同类产品和所有未指定物品必须保持原样。只返回一张完整、无接缝的 1086×1448 PNG。
