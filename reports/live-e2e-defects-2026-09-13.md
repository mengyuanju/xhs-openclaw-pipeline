# 真实端到端测试缺陷清单

> 对应执行台账：`reports/live-e2e-execution-2026-09-13.md`
> 规则：所有问题均保留编号和历史；修复后填写根因、修复版本、复测范围和复测结果，不删除原记录。

## 1. 缺陷统计

| 等级 | 新建 | 修复中 | 待复测 | 已关闭 | 阻塞 |
| --- | ---: | ---: | ---: | ---: | ---: |
| P0 | 0 | 0 | 0 | 0 | 0 |
| P1 | 11 | 0 | 0 | 0 | 2 |
| P2 | 15 | 0 | 0 | 0 | 0 |
| P3 | 1 | 0 | 0 | 0 | 0 |

## 2. 已知外部阻塞

### E2E-BUG-001：DeepSeek 真实调用返回 HTTP 402

| 字段 | 内容 |
| --- | --- |
| 等级 | P1 |
| 状态 | 阻塞，本轮已复测 |
| 模块 | 真实联网研究提供方 |
| 影响 | DeepSeek 不能完成真实模型输出，不能作为正式可用提供方 |
| 本轮证据 | 36 条主任务首次真实调用全部到达 DeepSeek，但均返回 HTTP 402；页面、执行记录和模型调用轨迹错误分类一致 |
| 处理要求 | 恢复提供方额度或计费状态后，用最小样本复测；通过前不能把 DeepSeek 配置为正式主提供方 |

### E2E-BUG-002：Dots 未配置真实调用密钥

| 字段 | 内容 |
| --- | --- |
| 等级 | P1 |
| 状态 | 阻塞 |
| 模块 | 真实文案提供方 |
| 影响 | 无法执行 Dots 真实文本调用 |
| 本轮证据 | 根环境和中心环境均未配置 `XHS_DOTS_API_KEY` |
| 处理要求 | 如正式生产需要 Dots，配置有效密钥后复测；禁止在仓库或报告中保存密钥 |

## 3. 本轮新发现

### E2E-BUG-003：项目数据库备份入口被迁移兼容校验阻断

| 字段 | 内容 |
| --- | --- |
| 等级 | P1 |
| 状态 | 新建；本轮先使用 PostgreSQL 原生只读导出建立恢复点 |
| 发现时间 | 2026-09-13，正式业务写入前 |
| 模块 | 数据库备份与迁移兼容 |
| 前置条件 | 当前分支 `codex/review-layout-polish`，提交 `2f9ce2e6f60cf996730321abff977de8b4d02bff`，中心数据库可正常提供服务 |
| 复现步骤 | 在项目根目录执行标准中心数据库导出入口 |
| 预期结果 | 创建只读一致性数据库备份并输出备份目录 |
| 实际结果 | 导出在迁移校验阶段停止，提示 `Migration 0043_xhs_search_node_retirement is missing or changed; use a compatible code/backup version.` |
| 影响 | 标准备份入口不可用；若不建立替代恢复点，不应开始破坏性或高风险 E2E 场景 |
| 初步判断 | 当前代码中的 0043 迁移文件与数据库已登记迁移的版本或校验值不一致，需核对部署代码与迁移历史 |
| 临时处理 | 使用同一数据库连接执行 PostgreSQL 原生只读 `pg_dump`，保留文件大小与 SHA-256 证据，不改动数据库 |
| 修复要求 | 恢复标准备份命令；验证迁移文件不可变性，并用备份—恢复演练复测 |

### E2E-BUG-004：产品宣称 Excel 批量入队，但词包导入仅支持文本和 CSV

| 字段 | 内容 |
| --- | --- |
| 等级 | P1 |
| 状态 | 新建 |
| 模块 | Query 词包导入 |
| 复现步骤 | 管理员进入 Query 词包，打开导入弹窗并检查文件选择能力 |
| 预期结果 | 可选择并解析方案及登录页宣称的 Excel 文件；错误行和重复行有明确反馈 |
| 实际结果 | 文件输入 `accept` 仅包含 `.txt,.csv,text/plain,text/csv`，帮助文案也只写纯文本或单列 CSV；真实 `.xlsx` 无入口 |
| 影响 | 无法按既定 Excel 批量入队流程执行真实业务；用户会被产品文案误导 |
| 修复要求 | 实现 `.xlsx` 单列/指定列解析、大小与行数保护、重复和空行统计，并补真实文件 E2E |

### E2E-BUG-005：作业员筛选详情混用全局统计和个人列表，范围不明确

| 字段 | 内容 |
| --- | --- |
| 等级 | P2 |
| 状态 | 新建 |
| 模块 | Query 词包筛选详情 |
| 实际结果 | 作业员只加载本人 15 条，但顶部显示“全部 60、待筛 60”，同页又显示“已加载 15/15”，没有标注全局/本人范围 |
| 影响 | 作业员会误判自己的剩余工作量，前端统计语义与当前列表范围不匹配 |
| 修复要求 | 将统计明确拆分为“全词包”和“我的分配”，或使顶部计数按当前账号范围展示 |

### E2E-BUG-006：陈旧筛选提交被服务端拒绝后，前端仍保留旧列表和可操作状态

| 字段 | 内容 |
| --- | --- |
| 等级 | P2 |
| 状态 | 新建 |
| 模块 | Query 词包并发筛选 |
| 复现步骤 | 页面 A 保持 15 条待筛；页面/接口 B 完成同一词包全部筛选；回到页面 A 暂存决策并提交 |
| 预期结果 | 冲突提示后自动重新读取，清空已失效暂存决策和操作按钮 |
| 实际结果 | 服务端正确返回“词包已结束，不能继续筛选”，但页面仍显示 15 条待筛和已暂存动作；关闭详情并手动刷新后才恢复 0 条待筛 |
| 影响 | 用户可能持续在无效数据上操作，状态流转与前端显示短时脱节 |

### E2E-BUG-007：词包完成后负责人显示为“仅管理员可筛选”，丢失真实分配历史

| 字段 | 内容 |
| --- | --- |
| 等级 | P2 |
| 状态 | 新建 |
| 模块 | Query 词包列表 |
| 实际结果 | 完成的词包曾分配给质检员及 3 名作业员各 15 条，但列表完成后显示“仅管理员可筛选” |
| 影响 | 前端历史语义错误，无法从列表判断真实参与人和分配结果 |
| 修复要求 | 完成后仍展示分配摘要或“已完成（4 人参与）”，不要复用未分配/仅管理员文案 |

### E2E-BUG-008：永久删除成功后列表数据清空，但总数和分页仍保留旧值

| 字段 | 内容 |
| --- | --- |
| 等级 | P2 |
| 状态 | 新建 |
| 模块 | 全部作业 / 永久删除 |
| 复现步骤 | 搜索唯一任务 #40，废弃后输入二级密码永久删除 |
| 预期结果 | 删除成功后列表、顶部命中数和分页总数同步变为 0 |
| 实际结果 | 列表显示“没有符合条件的作业”，但顶部仍为“共 1 条”、分页仍为“共 1 条”；手动刷新后变为 0 |
| 影响 | 删除结果与数量显示矛盾，容易让管理员怀疑删除未生效 |

### E2E-BUG-009：独立盲评可被质检员通过通用文案页面旁路识别

| 字段 | 内容 |
| --- | --- |
| 等级 | P1 |
| 状态 | 新建 |
| 模块 | 权限 / 独立盲评 |
| 前置条件 | 开启独立盲评，质检员存在待抽检样本 |
| 复现步骤 | 质检员先查看文案质检匿名队列，再打开导航中的“全部文案任务” |
| 预期结果 | 盲评期间质检员无法通过其他页面或 API 获取可关联的任务号、Query、词包和上游身份 |
| 实际结果 | “全部文案任务”直接显示任务号、Query、词包名称、负责人和创建人；最终稿标题可与匿名样本对应 |
| 影响 | 独立盲评可以被轻易去匿名化，质检结论可能受上游人员和批次信息影响 |
| 修复要求 | 服务端按盲评参与者和冻结范围统一脱敏/禁止旁路；不能只在 `/copy-qa` 组件层隐藏 |

### E2E-BUG-010：单条打回后显式放行同批其余任务必然 500

| 字段 | 内容 |
| --- | --- |
| 等级 | P1 |
| 状态 | 新建 |
| 模块 | 文案抽检异常放行 / 数据库迁移 |
| 复现步骤 | 抽检中仅打回一个样本；在成功提示中选择“放行同批其余”；填写说明并输入匿名样本编号确认 |
| 预期结果 | 错误项继续返工，其余待定样本进入图片队列；冻结批次记录 `RELEASED_WITH_EXCEPTIONS` |
| 实际结果 | UI 返回 `control plane request failed`，HTTP 为 500；直接诊断得到 PostgreSQL `22001`：值对于 `varchar(20)` 过长 |
| 根因证据 | `production_batches.status` 与 `copy_sampling_freezes.status` 均为 `varchar(20)`，但约束和代码写入 24 字符状态 `RELEASED_WITH_EXCEPTIONS` |
| 影响 | 单条最小影响策略无法即时放行其余任务，整个批次被不必要阻塞；前端还丢失底层明确错误 |
| 临时结果 | 其余样本逐条通过、错误项完成强制复检后，批次可走普通 `RELEASED` 闭合；不能替代异常放行修复 |
| 修复要求 | 新迁移扩大两处状态字段并补迁移回归；同时让前端展示稳定错误码和可行动提示 |

### E2E-BUG-011：已妥善提示的健康注意事项被图片自动质检当成阻断级风险

| 字段 | 内容 |
| --- | --- |
| 等级 | P1 |
| 状态 | 新建 |
| 模块 | 图片自动质检 / 文案风险字段 / 终审证据展示 |
| 复现数据 | 任务 #1“跑步前后热身与拉伸”；真实 Codex 文案、4 张真实 PNG、非模拟图片运行 |
| 预期结果 | 已在正文中明确给出疼痛停止训练、受伤/患病/怀孕咨询专业人员等安全边界的普通健康注意事项，不应自动等同于“严重合规问题”；若确有阻断风险，应展示具体风险内容和可行动建议 |
| 实际结果 | 文案元数据 `riskFlags` 仅为“运动建议存在个体差异；正文已加入疼痛停止训练及特殊人群咨询专业人员的安全边界”，但机械规则把任意非空 `riskFlags` 直接判为阻断，图片总体仅 1/3 分；终审 UI 只显示“模型标记 1 条风险”，看不到原始风险文本 |
| 矛盾证据 | 同一质检结果的模型证据认为 4 图清晰完整、文字与参考一致、构图和平台适配均为 3 分；机械证据却将图片基础质量、图中文字质量和信息价值压为 1 分，并给出“安全合规-严重问题” |
| 影响 | 涉及运动、健康、安全边界的正常内容可能系统性被误拦；审核员缺少具体证据，无法判断应改文案、改图片还是仅确认风险已被妥善披露 |
| 修复要求 | 区分“已披露的一般注意事项”和“未解决的阻断风险”；规范生成端 `riskFlags` 语义或增加严重度；自动质检展示原始风险文本并避免机械证据与模型证据互相矛盾；用健康、运动、宠物和普通生活内容回归 |

### E2E-BUG-012：多图片执行机共享 Codex 并发池时在 99% 阶段超时失败

| 字段 | 内容 |
| --- | --- |
| 等级 | P1 |
| 状态 | 新建；测试环境已从 3 台降为 1 台继续观察 |
| 模块 | 执行机并发协调 / Codex 本地并发队列 / 图片失败恢复 |
| 前置条件 | 3 个真实图片执行节点同时各运行 1 条图片任务，并共享同一台主机、同一 Codex 登录及本地并发池；同时曾有 1 条文案重试 |
| 复现数据 | 任务 #19“手机人像拍照构图”，4 页真实生图 |
| 预期结果 | 系统按实际可用 Codex 并发容量限流，或在内部排队/等待时持续可恢复重试；不应在图片已基本完成后整套失败 |
| 实际结果 | 任务运行 20 分 13 秒到 99% 后进入 `IMAGE_FAILED`，错误为 `Codex CODEX_QUEUE_TIMEOUT: local concurrency queue timed out`；3 个执行节点此前仍被中心同时视为各有 1 个图片槽位并继续派单 |
| 影响 | 多节点部署可能超过共享 Codex 账户/主机的真实并发上限，造成接近完成时失败、重复生图成本和队列吞吐雪崩；管理页显示的“可用槽位”不能代表实际模型并发容量 |
| 已验证保护 | 失败记录、耗时、99% 进度和检查点均保留，前端显示可人工续跑；不等于解决并发容量问题 |
| 修复要求 | 增加跨节点共享的 Codex 并发令牌或明确每主机/每账户并发配置；队列等待不应计入不可恢复超时，必要时退避续租；增加 2/3/4 节点真实并发回归和费用幂等检查 |

### E2E-BUG-013：删除词包后交付行仍显示词包快照，但上传范围被归为“历史未归属内容”

| 字段 | 内容 |
| --- | --- |
| 等级 | P2 |
| 状态 | 新建 |
| 模块 | 交付池 / 词包删除后的前端分类 |
| 复现数据 | 已永久删除源词包后，终审通过的任务 #22、#3；任务仍保留词包快照名 `E2E真实全流程-20260913-主词包` |
| 预期结果 | 同一交付条目的表格词包列、筛选项、整包上传范围使用一致分类；若快照词包仍可用于交付分组，应显示同名快照分组并明确“源词包已删除” |
| 实际结果 | 表格词包列继续显示原词包名，但“选择要上传预览的范围”把同一条目归为“历史未归属内容／早期未绑定词包的交付项”；任务 #22 与 #3 均稳定复现 |
| 影响 | 管理员可能把删除后仍有明确来源的内容误当成早期无归属数据，整包上传范围与列表筛选语义不一致 |
| 修复要求 | 统一服务端分组键与前端展示来源；区分“源词包已删除但有快照”和“从未绑定词包”，并补词包删除后交付/上传回归 |

### E2E-BUG-014：图片自动质检对明显透明毛边和噪点给出 3 分且无问题

| 字段 | 内容 |
| --- | --- |
| 等级 | P1 |
| 状态 | 新建 |
| 模块 | 图片自动质检 / 透明边缘检查 |
| 复现数据 | 任务 #25“考研每日学习计划”和任务 #26“上班族下班后学习方法”，均为真实 Codex 4 页 PNG；在白底、棋盘格和深底检查 |
| 预期结果 | 大面积异常透明、标题/背景毛边和噪点应被自动质检识别，至少降分并定位到第 1 页；前端应提示人工切换底色复核 |
| 实际结果 | 两套图自动质检均为 3/3 分、0 个问题。任务 #25 第 1 页有大面积异常透明、毛边与噪点，人工评 2 分返工；任务 #26 第 1 页被识别为含透明像素，白底可读但深底下整页变暗、标题几乎不可读，而且同套其余 3 页均为不透明图，人工评 1 分并废弃 |
| 影响 | 不合格成品可被自动门禁当成优质图片，若人工未逐底色查看可能直接进入交付；与 E2E-BUG-011 的误报同时存在，说明当前质检既有假阳性也有假阴性 |
| 修复要求 | 对 alpha 覆盖率、边缘颜色污染、透明区域噪点和文字边缘做机械检测；模型检查必须看到实际交付合成效果；以任务 #25 原图做固定回归样本 |

### E2E-BUG-015：交付条目因返工撤回后，已发布公网预览仍可继续访问

| 字段 | 内容 |
| --- | --- |
| 等级 | P1 |
| 状态 | 新建；测试人员已对唯一测试预览执行定点撤销，避免继续公开 |
| 模块 | 交付状态机 / 预览服务撤销 |
| 复现数据 | 任务 #22 已终审、交付 `READY`、成功发布公网预览；随后在任务详情点击“重新生成图片” |
| 预期结果 | 任务进入返工/待生图且交付变为 `WITHDRAWN` 时，同一事务或可靠补偿任务撤销公网预览；外链立即不可访问，前端显示已撤销 |
| 实际结果 | 内部任务从 `REVIEWED` 变为 `IMAGE_QUEUED`，交付从 `READY` 变为 `WITHDRAWN`，交付池归零；但数据库预览状态仍为 `PUBLISHED`，原公网链接刷新后继续显示旧文案和 4 张旧图 |
| 风险 | 已认定需要返工、可能包含错误或合规风险的旧内容仍公开可访问，内部状态和外部展示严重不一致 |
| 临时处理 | 仅针对测试任务 #22 调用预览撤销接口并把预览状态记为 `REVOKED`；复查外链已显示“Preview unavailable”。这只是测试清理，不是产品自动补偿 |
| 修复要求 | 将交付撤回与预览撤销做成可靠状态机；失败需重试、告警并在 UI 明示“撤回中/撤回失败”；覆盖重新生图、三种返工、废弃、版本失效和永久删除 |

### E2E-BUG-016：图文终审可无原因发起返工，下游却要求作业员“按返工原因修改”

| 字段 | 内容 |
| --- | --- |
| 等级 | P1 |
| 状态 | 新建 |
| 模块 | 图文终审返工 / 审核指令传递 / 审计 |
| 复现数据 | 任务 #4 图片人工评 3 分后选择“仅文案返工” |
| 预期结果 | 任何返工都必须至少选择结构化原因或填写具体修改要求；作业员能看到审核员、原因、页码/字段和备注 |
| 实际结果 | 3 分图集可直接选择返工范围并确认，没有原因或备注输入；作业员页面显示“按返工原因修改”，但状态卡既无原因也无要求，只能自行猜测并做任意修改 |
| 影响 | 返工没有可执行验收标准，操作员可能无效修改后再次进入强制复检；审计记录无法解释为什么退回及是否已修正 |
| 修复要求 | 三种返工范围均强制填写原因与备注；文案返工定位字段，图片返工定位页码；强制复检页应展示必要的匿名化修改要求并验证实际修正 |

### E2E-BUG-017：仅文案返工会把已开启的图片水印配置静默重置为关闭

| 字段 | 内容 |
| --- | --- |
| 等级 | P1 |
| 状态 | 新建 |
| 模块 | 图片配置版本继承 / 文案返工状态流转 |
| 复现数据 | 任务 #4 首次真实图片运行的终审页明确显示“AI生成水印 已开启”；执行“仅文案返工” |
| 预期结果 | 只修改文案时，图片格式、背景和水印设置应从上一个有效图片版本继承；如必须重置应要求用户明确确认 |
| 实际结果 | 作业员打开文案返工页时水印已变为“关闭”，未触碰图片配置；返工稿通过强制复检后，管理员看到待生图任务仍为“已关闭”，即新一轮图片将静默丢失原水印 |
| 影响 | 文案层面的修改改变了交付图片合规配置，前后批次外观不一致；作业员容易在不知情下生成无水印版本 |
| 修复要求 | 新文案修订继承当前有效 `imageSettings`；保存/提交/强制复检前后断言不变；增加仅文案、仅图片、双返工和重新生成四类配置传播测试 |

### E2E-BUG-018：统计页把中间源图计入“有效产出图片”，交付图数量被重复计算

| 字段 | 内容 |
| --- | --- |
| 等级 | P2 |
| 状态 | 新建 |
| 模块 | 统计口径 / 图片资产 / 前端指标展示 |
| 复现数据 | 已终审并进入交付池的任务 #3；当前图片运行声明 4 张结果图，终审页显示 4 张，交付 ZIP 也只包含 4 张 |
| 预期结果 | “期间有效产出图片”只统计当前有效、可交付的 4 张成品图；中间源图、历史版本和已作废运行不应计入交付产出 |
| 实际结果 | 统计页显示“期间有效产出图片 8”；数据库同一图片运行有 8 条资产，其中 4 条为最终图，另 4 条文件名带 `source-`、属于生成中间源图，因此指标恰好翻倍 |
| 状态一致性 | 任务状态为 `REVIEWED`，交付为 `READY`，终审页面、当前运行结果和交付包均一致为 4 张；只有统计卡片显示 8 张 |
| 影响 | 管理看板高估真实可交付图片产量；含源图、多轮重试或历史运行的任务可能产生更大偏差，影响产能、成本和质量判断 |
| 修复要求 | 以当前有效图片运行的 `result.images` 或交付绑定的资产 ID 为统计真源；明确排除 `source-*`、历史运行、中间文件和已作废资产；增加 4 张成品+4 张源图、重试及返工后的统计回归 |

### E2E-BUG-019：文案质检页在 375px 手机视口发生整页横向溢出

| 字段 | 内容 |
| --- | --- |
| 等级 | P2 |
| 状态 | 新建 |
| 模块 | 文案质检前端 / 响应式布局 |
| 复现环境 | 真实管理员账号；浏览器视口 375×812，页面可用宽度 360px；访问 `/copy-qa` |
| 预期结果 | 页面外壳保持在 360px 可用宽度内；确需横向展示的表格应只在带可访问名称的表格容器内部滚动，页头、摘要卡和正文不应整体被截断 |
| 实际结果 | 文档宽度从 360px 扩大为 435px，出现浏览器级横向滚动条；摘要区固定为 419px，正确率面板右边达到 435px，第二列摘要、表格“正确率”及底部统计标签均在首屏右侧被裁掉，必须拖动整页才能查看 |
| 对照证据 | 同一 375×812 视口下，个人信息、用户、执行机、词包、交付池、统计、配置、提示词、知识库及各作业列表的文档宽度均为 360px；统计表的横向滚动被正确限制在标注为“作业员明细，可横向滚动”的内部容器 |
| 影响 | 质检员在手机或窄窗口无法同时理解摘要和正确率，页面整体横移还会让导航、标题和操作区一起离开视口；属于核心审核页面的主要可用性问题 |
| 修复要求 | 取消摘要/面板在窄屏下的固定最小宽度，摘要卡改为单列或可换行；表格使用独立 `overflow-x:auto` 容器并提供可访问名称；在 375、768、1440px 下回归页面宽度、表头和底部标签 |

### E2E-BUG-020：交付池在 768px 平板视口被固定面板宽度撑出页面

| 字段 | 内容 |
| --- | --- |
| 等级 | P2 |
| 状态 | 新建 |
| 模块 | 交付池前端 / 响应式布局 |
| 复现环境 | 真实管理员账号；浏览器视口 768×1024，可用宽度 753px；桌面侧栏占 252px，内容区可用 501px |
| 预期结果 | 交付工具栏、搜索、范围选择和列表应在 501px 内容区内换行或收缩；若局部内容必须滚动，只允许局部容器横向滚动 |
| 实际结果 | 页面文档宽度被撑到 866px；交付面板固定约 590px，右边超出视口 113px，工具栏、搜索框和预览范围一起被裁掉，出现整页横向滚动条 |
| 影响 | 平板和窄桌面窗口下，上传、Excel、ZIP、范围筛选等交付核心操作无法在同一视口中完整查看，容易漏选或点错范围 |
| 修复要求 | 在侧栏仍展开的中等断点下取消面板/工具栏最小宽度，按钮与字段允许换行；为真正需要宽度的列表提供独立滚动区；回归 768、1024、1440px 和侧栏开关状态 |

### E2E-BUG-021：用户管理在 768px 平板视口同时出现表格内外双重横向滚动

| 字段 | 内容 |
| --- | --- |
| 等级 | P2 |
| 状态 | 新建 |
| 模块 | 用户管理前端 / 响应式表格 |
| 复现环境 | 真实管理员账号；浏览器视口 768×1024，可用宽度 753px；访问 `/users` |
| 预期结果 | 用户表可在自身 451px 容器内横向滚动查看 667px 表格，页面外壳和标题、统计卡、其他区域保持在视口内 |
| 实际结果 | 用户表自身已有横向滚动条，但其 667px 表格仍把整个文档宽度扩到 905px；右侧“操作”列完全出屏，浏览器底部又出现第二条整页横向滚动条 |
| 影响 | 用户启停、编辑、重置密码和删除等高风险操作位于屏外；双重滚动让管理员难以确认当前行与操作，增加误操作风险 |
| 修复要求 | 让 `.table-wrap` 建立有效的宽度约束（如 `min-width:0`、`max-width:100%` 和正确 overflow 包含关系），表格只在容器内滚动；回归当前账号、长账号名和三按钮操作列 |

### E2E-BUG-022：“跳到主要内容”链接只改变地址锚点，没有把键盘焦点移入主内容

| 字段 | 内容 |
| --- | --- |
| 等级 | P2 |
| 状态 | 新建 |
| 模块 | 全站外壳 / 键盘无障碍 |
| 复现环境 | 真实管理员账号，1920×1080；刷新全部作业页后从页面起始位置按 Tab，再按 Enter 激活“跳到主要内容” |
| 预期结果 | URL 进入 `#main-content` 的同时，键盘焦点落到主内容容器或首个主标题；下一次 Tab 从主内容内继续，跳过整段侧栏导航 |
| 实际结果 | 地址正确变为 `#main-content`，但 `document.activeElement` 变为 `BODY`；`main#main-content` 没有 `tabindex`，焦点没有进入主内容，键盘用户仍需重新穿过导航控件 |
| 对照证据 | 同页“创建笔记”弹窗的焦点管理正常：打开后焦点进入文本域，按 Esc 关闭后焦点返回“创建笔记”按钮 |
| 影响 | 跳转链接视觉上生效但没有完成键盘绕过导航的核心用途，不符合键盘可访问性预期 |
| 修复要求 | 让 `main#main-content` 可程序化聚焦（通常 `tabIndex=-1`），激活跳转后显式聚焦并保留可见焦点；在桌面/手机、前进后退和弹窗关闭后回归 Tab 顺序 |

### E2E-BUG-023：图片三次失败耗尽后重新通过文案审核，会卡入已结束批次的待质检状态

| 字段 | 内容 |
| --- | --- |
| 等级 | P1 |
| 状态 | 新建；保留任务 #23 现场用于修复后复测 |
| 模块 | 图片重试耗尽 / 文案重审 / 生产批次与抽检状态机 |
| 复现数据 | 任务 #23“新手相机参数入门”；原文案修订 #53 已人工 3 分、所属生产批次 #4 已 `RELEASED/COMPLETED`，抽检项 #22 已 `PASSED` |
| 故障注入 | 使用真实中心与真实执行机协议，将执行机工作目录定点设为仓库外的文件路径；同一任务三次正常领取并分别以 `ENOTDIR` 失败。前两次显示第 1/2 次并自动排队，第三次准确进入 `IMAGE_RETRY_EXHAUSTED` |
| 预期结果 | 人工在耗尽页确认原文案仍为 3 分后，应直接建立新的图片执行资格并回到 `IMAGE_QUEUED`；如业务要求再次抽检，则必须创建可处理的新强制复检项 |
| 实际结果 | 点击“审核通过并进入后续流程”后，任务变为 `COPY_QC_PENDING`，阶段标记也被覆盖为普通 `COPY_QC_PENDING`，前端提示“等待生产批次完成初审并进入文案抽检”；但原批次已经完成、原抽检项已经通过，文案质检页没有该任务，全部作业也没有重新生图入口 |
| 数据状态矛盾 | 任务等待批次/抽检；同一修订对应批次为 `RELEASED`、sampling 为 `COMPLETED`、freeze 为 `RELEASED`、sample 为 `PASSED`。前端提示的前置条件永远不会再次发生 |
| 影响 | 真实图片连续失败后，即使人工确认文案没有问题，任务仍永久卡死；只能越过正常页面直接改数据或增加特殊后台操作，核心恢复链路中断 |
| 修复要求 | 在图片耗尽重审路径识别已结束批次和已通过修订，直接重新排队图片；若需强制复检则原子创建新项并保留 `IMAGE_RETRY_EXHAUSTED` 来源；增加三次失败→重审 3 分→待生图→新执行，以及 2/2.5 分改稿后重审的真实状态回归 |

### E2E-BUG-024：上传中断后重试会重复保存已成功上传的图片，并保留失败运行的孤立资产

| 字段 | 内容 |
| --- | --- |
| 等级 | P2 |
| 状态 | 新建；保留任务 #33 两个图片运行和资产记录用于修复后复测 |
| 模块 | 图片上传补偿 / 资产幂等 / 存储与统计口径 |
| 复现数据 | 任务 #33“猫咪饮水量观察记录”；真实 Codex 4 页图片，执行 `1eeb36bd…` 在 `UPLOADING/97%` 时中断中心连接 |
| 预期结果 | 已成功接收的文件按执行、文件名和 SHA-256 幂等确认；重试只补传缺失文件，失败运行不留下会被业务统计或下载读取的孤立资产 |
| 实际结果 | 中断前服务已保存第 1 张 `01-hero.png`；同节点补偿重试建立新运行 `dfb99ccf…` 后又保存相同 SHA-256 的第 1 张并上传其余 3 张。数据库最终保留失败运行下 1 条资产和成功运行下 4 条资产，同一任务共 5 条，其中两条第 1 张字节与哈希完全相同 |
| 已通过保护 | 补偿执行约 1 秒完成，新执行没有任何模型调用；4 个本地源文件哈希均未改变，当前成功运行和前端只展示正确的 4 张成品 |
| 影响 | 重试次数越多，失败运行资产和对象存储占用越多；若统计或清理以任务级资产为口径会重复计数，且与 E2E-BUG-018 叠加放大产量偏差 |
| 修复要求 | 上传接口增加稳定幂等键并返回已存在结果；完成补偿时迁移/复用已成功资产或将失败运行资产标记为非有效并可靠清理；统计和交付只读取当前有效运行；增加首张、末张及完成回报中断回归 |

### E2E-BUG-025：图片失败恢复成功后，任务列表和统计只展示续跑耗时，丢失整个生产耗时

| 字段 | 内容 |
| --- | --- |
| 等级 | P2 |
| 状态 | 新建 |
| 模块 | 图片恢复状态汇总 / 作业列表 / 生产耗时统计 |
| 复现数据 | 任务 #33 原运行约 10 分 54 秒后上传失败，补偿运行约 1 秒；任务 #19 原运行 20 分 13 秒失败，检查点续跑 2 分 5 秒成功 |
| 预期结果 | 列表明确区分“本次续跑耗时”和“累计图片生产耗时”，统计产能至少使用包含失败与恢复阶段的累计时长；完整执行历史可从详情追溯 |
| 实际结果 | #33 恢复后列表显示开始时间 21:07:10、耗时 1 秒；#19 恢复后显示开始时间 21:13:09、耗时 2 分 5 秒。前一失败运行的 10～20 分钟被任务汇总字段覆盖，用户会认为整套真实图片只用 1 秒或 2 分钟生成 |
| 数据状态 | `task_executions` 中失败和成功运行的开始/结束时间均完整保留，但任务级 `execution_started_at` 与前端只指向最后一次续跑；当前状态和图片版本正确，耗时语义错误 |
| 影响 | 生产效率、容量和费用判断被系统性低估；上传补偿越快，误差越大，可能掩盖模型超时和失败成本 |
| 修复要求 | 增加累计耗时或恢复链聚合口径，并在 UI 区分累计/本次；统计按同一恢复链汇总执行时间，避免重复计算重叠区间；用上传补偿和中途图片修复两种样本回归 |

### E2E-BUG-026：大数据量下尾页、模糊查询、执行机列表和前端作业列表响应超出门槛

| 字段 | 内容 |
| --- | --- |
| 等级 | P2；目标容量发布门禁，压力容量为趋势风险 |
| 状态 | 新建 |
| 模块 | PostgreSQL 查询 / 执行机聚合 / 全部作业前端 |
| 复现环境 | 隔离性能库；10,000、50,000、100,000 任务逐级扩容，最终为 500 个用户、100 个执行节点；无真实模型调用 |
| 暂定门槛 | 普通读取 API p95 不超过 300ms、p99 不超过 800ms；前端 LCP 不超过 2.5s，筛选应快速反馈且只显示最新请求结果 |
| 基准结果 | 10,000 任务下所有已测普通读取 p95 不超过约 103ms，错误率 0；60 次单条创建 p95 18.1ms，100 条批量创建 62.1ms，写入门槛通过 |
| 目标容量结果 | 50,000 任务下尾页和执行机聚合已经跨过 300ms 读取门槛；请求均成功，没有丢数据或重复行 |
| 压力容量结果 | 100,000 任务下，尾页 p95 1,131.2ms/p99 1,155.9ms，中文 Query 查询 p95 588.5ms/p99 694.5ms，执行机列表 p95 1,709.8ms；健康检查 p95 99.4ms，错误率仍为 0 |
| SQL 证据 | 尾页执行计划对 100,000 行顺序扫描并排序，单次约 138.3ms；执行机聚合对 100 个节点执行相关子查询循环，复测单次约 1,427.9ms、约 912,702 次缓冲命中；完整 30 分钟压测后 PostgreSQL 累计产生 3,432 个临时文件、约 37.99GB 临时写入，死锁为 0 |
| 前端证据 | 100,000 任务下“全部作业”三次热加载约 3.9～4.3s，正确显示 100,000 条/5,000 页；`#100000` 精确筛选约 3.3s，尾页跳转约 2.6s，结果、状态和数量均正确。100 行切换的快照采样超过工具时限，判为测量无效，不以此声称页面卡死 |
| 影响 | 数据量增大后管理员列表、筛选和执行机管理等待明显；相关查询会产生大量排序和重复扫描，可能与在线写入竞争数据库资源 |
| 修复要求 | 用基于稳定排序键的游标分页替代大 offset；为 Query 搜索建立合适索引或搜索方案；将执行机统计改为一次分组聚合/物化摘要，消除每节点相关扫描；减少前端首屏并行重复请求并补测真实 LCP/INP；在 50,000 目标档和 100,000 压力档分别回归 |

### E2E-BUG-027：全新目录只执行根目录安装后，项目声明的全量类型检查无法运行

| 字段 | 内容 |
| --- | --- |
| 等级 | P2 |
| 状态 | 新建 |
| 模块 | 安装文档 / 依赖边界 / 全新构建 |
| 复现环境 | 从当前冻结提交克隆到全新临时目录；Node.js 24.19.0；不复用原工作区 `node_modules` |
| 预期结果 | 按根目录文档执行一次 `npm ci` 后，根脚本 `npm run typecheck:all` 可完成主站和独立预览服务类型检查；或者文档和脚本必须明确给出第二个安装步骤 |
| 实际结果 | 根目录 `npm ci` 成功且审计为 0，但随后 `npm run typecheck:all` 在预览服务因缺少 `@cloudflare/workers-types` 和 `vinext/types` 失败；再执行 `npm --prefix preview-service ci` 后，同一全量类型检查和生产构建均通过 |
| 影响 | 新机器、CI 或发布人员按项目根入口操作会把依赖不完整误判为代码失败，或跳过预览服务检查；构建结果依赖隐含的第二次安装 |
| 修复要求 | 选择 npm workspaces、根安装脚本或明确文档之一，保证标准全新安装后全量检查可重复；CI 从空缓存执行并同时构建主站与预览服务 |

### E2E-BUG-028：独立预览服务开发依赖仍包含 4 个中等级别漏洞

| 字段 | 内容 |
| --- | --- |
| 等级 | P3；生产依赖审计为 0 |
| 状态 | 新建 |
| 模块 | 独立预览服务 / 开发工具供应链 |
| 复现环境 | 全新安装后在 `preview-service` 执行完整依赖审计 |
| 实际结果 | 共 4 个 moderate：直接开发依赖 `drizzle-kit` 经 `@esbuild-kit/esm-loader`、`@esbuild-kit/core-utils` 引入 `esbuild <=0.24.2`，关联 GHSA-67mh-4wv8-2f99；`npm audit --omit=dev` 为 0 |
| 影响 | 不影响已安装的生产依赖，但在不可信网络暴露旧版开发服务器时存在跨站请求读取风险；自动修复建议涉及版本回退/主版本边界，不能盲目执行 |
| 修复要求 | 评估升级到不含旧 esbuild 链的 drizzle 工具版本；开发服务器只绑定可信地址；升级后回归迁移生成、类型检查、构建和预览烟测 |

### E2E-BUG-029：客户端错误虽返回请求 ID，但中心正常/四百错误日志没有对应链路记录

| 字段 | 内容 |
| --- | --- |
| 等级 | P2 |
| 状态 | 新建 |
| 模块 | 中心服务可观测性 / 故障定位 |
| 复现环境 | 隔离中心；分别发送健康请求和未认证写请求，记录响应 `X-Request-Id` 后检查中心标准输出 |
| 预期结果 | 客户端获得的请求 ID 能在中心结构化日志中检索，并在任务/执行相关请求中同时包含 taskId、executionId、阶段、状态码和耗时；日志不得包含 Cookie、Token、密码或完整敏感提示词 |
| 实际结果 | 两次响应都生成了新的 UUID 请求 ID并正确返回 `no-store`/`nosniff` 等头，但中心标准输出没有任一请求的访问记录；源码仅在映射为 500 时直接 `console.error(error)`，普通成功和 4xx 均不记录请求 ID |
| 影响 | 页面让用户提供请求 ID 后，管理员无法从当前中心日志定位相应请求；权限拒绝、慢请求和部分外部错误缺少统一链路，真实故障只能依赖数据库审计或复现 |
| 修复要求 | 增加脱敏结构化访问日志，至少记录时间、requestId、方法、规范化路由、状态码、耗时及可用的 task/execution 标识；5xx 日志也通过统一脱敏器，禁止直接输出秘密字段；补充日志关联和秘密扫描自动测试 |
