# Web 作业统计交付记录

- [x] 核对现有列表、执行详情、审核时间及旧统计数据源。
- [x] 提出个人概览、管理员统计页、指标口径和现有接口下的限制。
- [x] 用户确认按所述缓存、限速和 Web 统计方案实施，覆盖个人概览和管理员统计。

- [x] 统计口径、分页聚合、明细复用、权限隔离和限速。
- [x] 个人概览、状态筛选、默认折叠的 7/30 天 ECharts 图表。
- [x] 管理员数量、人员、效率、失败占比、产出及长期停留页面。
- [x] 假数据行为测试、真实本地只读接口验证、生产构建与浏览器检查。

验证详见 `verification.md`。以下保留原验收口径，均按已确认范围完成。

## 1. Web 统计口径与只读聚合（M）

依赖：方案评审。预计修改 Web 专属统计工具、Web 统计路由及测试，共 3–4 个文件；不修改中心服务和执行机。

验收：个人数据由会话绑定；时间按上海时区；去重、废弃、零样本和部分失败口径正确；普通账号不能读取管理员指标。

验证：跨午夜、跨账号、超过 200 条、数据缺失与分页变动的假数据测试；所有请求均为对现有中心接口的只读请求，不调用模型。

## 2. 个人概览（M）

依赖：1。预计修改个人概览组件、工作台接入、样式和图表加载组件，共 3–4 个文件。

验收：今日和累计创建/完成卡片、当前状态数字、可展开趋势图；切换列表页码不改变总统计；查看统计不挡住列表操作。

验证：浏览器验证普通账号、空数据、统计失败、320/768/1024/1440 宽度及键盘操作。

## 3. 管理员数量与人员明细（M）

依赖：1。预计修改管理员统计页、人员表、导航与测试，共 4–5 个文件。

验收：期间与累计口径明确；账号/角色筛选一致；无归属任务独立显示；人员表可排序并查看对应作业。

验证：管理员、审核员、普通用户的页面与接口权限；人员合计与总体的范围一致，当前角色不被描述为历史角色。

检查点：第一阶段数量、权限、性能与列表交互验证通过。

## 4. 管理员效率分析（M）

依赖：1、3。预计修改效率聚合、缓存、图表和测试，共 4–5 个文件。

验收：分开计算文案/图片成功执行耗时，展示样本数与中位数；生成与总交付耗时不混淆；请求受限且数据不完整时有提示。

验证：失败、放弃、重试、缺失起止时间、多张图片、有效/历史图片运行的假数据；检查执行快照不会进入图表响应。

## 5. 最终验证与文档（S）

依赖：2、3、4。

验收：Web 构建和相关测试通过；加载和刷新成本已测量；变更清单不含 `server/`、执行机、队列、数据库迁移或模型流程修改。

验证：真实浏览器、只读本地服务测试及 Git 差异检查，保留既有服务配置和运行模式。
