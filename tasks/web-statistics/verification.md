# Web 作业统计验证

2026-09-06，分支 `auto-clow-poker`，基线 `952640c`。

## 功能与边界

- 个人数量来自会话账号的全部分页数据，独立于任务列表页码和搜索；今日按上海时区划分，累计含废弃，审核完成按当前有效审核时间。
- 管理员汇总期间产出、当前积压、人员数量、执行耗时均值/中位数/P90、失败占比、多次执行占比、有效图片和长期未更新作业。
- 数量失败时保留上次完整结果和更新时间，冷加载不显示部分总数。详情缺失时标明未完整和已读取覆盖数，无有效耗时显示暂无样本。
- 只读现有中心 GET 列表和 GET 单任务详情。没有新增中心接口、写操作、数据库连接、后台任务或模型调用。
- 修改边界为 app、src/web-statistics、Web 代理权限及测试、ECharts 依赖和本任务文档；不修改 server、src/executor、队列或模型流程。

## 自动化与接口检查

- 统计新增 19 项行为测试：上海午夜、日期范围、任务去重、取消与有效审核口径、无归属人员、均值/中位数/P90、模拟/失败/放弃/无效耗时、图片运行归属、敏感字段剔除。
- 缓存与调度：201 项完整分页、分页移动有界重读、并发请求合并、每秒最多一次且并发一、队列上限及失败释放、过期身份清缓存、跨账号/中心/凭据版本隔离、更新时间复用与状态变化立即失效、错误保留旧结果、超限不伪造总量。
- Web 代理测试覆盖三种角色的页面边界和精确 API 路径；直接请求本地 Web API 验证 USER/REVIEWER 不能读取 admin/details 或管理员页面（403），匿名 401，无效日期及未知参数 400。
- 普通账号传入其他账号查询值仍只返回自己的 4 项本地测试作业，响应不包含人员表、其他创建者或效率数据。
- 根测试套件 833/833、TypeScript 检查和生产构建通过。旧工作台来源断言调整为允许“视图 + 跳转筛选”共同构成组件重置键，保留视图切换隔离要求。
- 仅重启本地 Web 服务，继续使用原本的本地测试配置与 3001 端口；4310 中心服务进程未变。重启后通过真实 Web API 再次验证 4 项数量和 4 项完整明细、2 条文案成功样本，响应无执行快照。

## 浏览器检查

- 本地现有测试夹具：共 4 项，当前待处理 3、已废弃 1、异常 3；人员表一致。明细分批完成，无浏览器警告/错误。
- 管理员角色筛选空结果显示 0；选择前一天后期间新增归零，累计创建仍为 4。自定义日期、近 7/30 天、人员排序搜索和作业跳转可用。
- 个人概览折叠/展开、近 30 天和已废弃筛选可用；统计失败不阻挡原任务列表。
- 320、768、1024、1440 宽度检查，页面无横向溢出，宽表局部滚动；图表随尺寸变化，键盘可操作筛选及折叠区域。
- 所有测试均使用假数据或现有本地夹具；未启动执行机，未新增真实生成任务，未消耗模型额度。

## 已知限制

- 1 次/秒是单 Web 进程新增统计读取的上限；多个 Web 实例的预算相加，原任务列表流量单独存在。
- 首次扫描每 200 项一页，管理员明细每项一次 GET。日期与人员切换复用事实；首次大范围效率分析可能较慢。刷新与详情进度可见。
- 中心分页没有一致性快照；此报表是近实时视图。20,000 项范围上限、12 个缓存范围和历史模拟来源局限详见 plan.md。
- 依赖审计的现有 exceljs/uuid 两项 moderate 问题未由本次 ECharts 引入，未扩展本次改动去升级其他依赖。
